{
  "scripts": {
    "generate:ts": "bunx twirpscript",
    "generate:go": "buf generate",
    "generate": "npm run generate:go && npm run generate:ts"
  },
  "dependencies": {
    "twirp-ts": "^2.5.0",
    "twirpscript": "^0.0.72"
  }
}