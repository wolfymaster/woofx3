# Build output — regenerated by `bun run build` and `bun run build:shim`,
# and packed by `npm publish` through the `files` field in package.json.
dist/

# Local install artefacts.
node_modules/
*.tgz
